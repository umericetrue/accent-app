<% if user_signed_in? %>
  <% if current_user.already_heiban?(@accent) %>
    <%= link_to '平板型', accent_heiban_path(@accent), method: :delete %>
  <% else %>
    <%= link_to '平板型', accent_heibans_path(@accent), method: :post %>
  <% end %>
  <%= @accent.heibans.count %>
<% else %>
  平板型 <%= @accent.heibans.count%>
<% end %>
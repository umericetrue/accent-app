<% if user_signed_in? %>
  <% if current_user.already_odaka?(@accent) %>
    <%= link_to '尾高型', accent_odaka_path(@accent), method: :delete %>
  <% else %>
    <%= link_to '尾高型', accent_odakas_path(@accent), method: :post %>
  <% end %>
    <%= @accent.odakas.count %>
<% else %>
  尾高型 <%= @accent.odakas.count%>
<% end %>